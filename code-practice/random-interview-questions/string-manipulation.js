// Basic and common interview questions regarding string manipulation to place something at the start and end of array

const myArray = ['a', 'b', 'c', 'd']

myArray.push('end')
myArray.unshift('start')

console.log(myArray)
